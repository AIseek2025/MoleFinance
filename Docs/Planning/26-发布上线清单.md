# 26 — 发布上线清单（Go-Live Checklist）

> 在 wave 28（就绪评估 + Overview 落地页）与 wave 29（协议 rollup 端到端接线）之后，本文件是**正式上线发布**的操作清单。分为「沙箱内已就绪」与「上线当天/部署期（沙箱外）必须人工执行」两部分。

最后更新：2026-06-19（wave 29）

---

## A. 沙箱内已就绪（代码 + 验证全绿）

- [x] 合约指令面 launch-complete（见 doc 25 §1）
- [x] 链下服务：clearing-core / harness / indexer / keeper-bot / keeper-rpc / ops-toolkit 全测试通过
- [x] ops-toolkit 22 项健康检查 + prober daemon
- [x] **协议级 rollup（wave 28+29）**：`ops_toolkit::protocol_summary` → 已接进 prober 的多市场 JSN（`"protocol"` 块）+ Prometheus（`mole_prober_markets{status=…}`）
- [x] **前端协议总览落地页（wave 28+29）**：`OverviewPanel` 默认 tab，聚合 TVL/OI/偏斜/恢复 + 消费 prober `protocol` 块显示协议健康横幅
- [x] 三个治理验证器全绿（test-counts / schema-parity / security-references）
- [x] `cargo test` 465 · 前端 vitest 154 · clippy clean（默认 + `solana-rpc`）· typecheck/build clean

## B. 部署期人工执行（沙箱外，按顺序）

### B1. 合约部署
1. [ ] 安装 Solana + Anchor 工具链（见 `programs/mole-option/src/lib.rs` BUILD NOTES）。
2. [ ] `anchor build` 产出 `target/deploy/mole_option.so`。
3. [ ] `anchor keys list` 获取真实 program ID，替换 `declare_id!` 占位与前端 `VITE_MOLE_PROGRAM_ID`。
4. [ ] devnet 部署：`anchor deploy --provider.cluster devnet`。
5. [ ] `anchor idl init <PROGRAM_ID>` 发布链上 IDL。
6. [ ] 初始化治理：`initialize_global_config`（admin / emergency / fee 多签）。
7. [ ] 初始化首个市场 + 子池 + 分发账本 + keeper-leader 锁。

### B2. 链下服务上线
8. [ ] keeper-bot：配置 `markets.toml`（program_id / market_pda / lock_pda / expected_leader），`serve-multi` 起 HA 实例。
9. [ ] ops-toolkit prober：设 `MOLE_OI_RPC_URL`（`solana-rpc` feature），起守护进程，确认输出 JSON `protocol` 块 + Prometheus `mole_prober_markets`。
10. [ ] Prometheus/AlertManager：接 `mole_prober_worst_exit_code` 与 `mole_prober_markets{status="critical"}` 告警规则。

### B3. 前端上线
11. [ ] 配置 `.env`：`VITE_RPC_URL` / `VITE_MOLE_PROGRAM_ID` / `VITE_MARKETS`(JSON) / `VITE_PROBER_SNAPSHOT_URL`。
12. [ ] 接入真实浏览器钱包（替换 `MockWalletAdapter` → `WindowWalletAdapter`，Phantom/Backpack）。
13. [ ] `npm run build` 部署静态资源（CDN）。
14. [ ] 烟测：Overview 落地页显示真实 TVL/OI + 协议健康横幅；Trader 真签名开/平仓。

### B4. 上线门禁（go/no-go）
15. [ ] **外部安全审计** firm 报告通过 + 高危项闭环（见 `SECURITY.md` 审计 onboarding 包）。
16. [ ] devnet 集成矩阵 7×24 跑通（无 P0/P1 告警）。
17. [ ] 应急演练：`pause_market` / `set_globally_paused` / `freeze_new_position` 全链路验证。
18. [ ] 主网部署 + 灰度（先小额 notional 上限），监控 24h 后放开。

---

## C. 上线后

- [ ] 持仓历史 / 已平仓 PnL 页（wave 30，依赖事件回放）。
- [ ] solana-client 版 `RpcAccountSource` shim 端到端真集群（替换 prober 的 DemoFetcher 基座）。
