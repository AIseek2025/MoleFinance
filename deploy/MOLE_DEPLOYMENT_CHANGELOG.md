# Mole Deployment Changelog

## 2026-06-20

- 新增 `deploy/` 全套 ECS 文档体系
- 新增 `deploy/scripts/` 前端发布脚本
- 新增 `infra/nginx/` 的 `molefinance.net` 配置
- 新增 `infra/scripts/setup-server.sh`
- 新增 `infra/systemd/mole-keeper-devnet.service`
- 新增 `frontend/.env.production.example`
- 前端增加 `VITE_DEFAULT_FEED=live` 支持
