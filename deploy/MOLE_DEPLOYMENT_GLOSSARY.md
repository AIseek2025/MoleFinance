# Mole Deployment Glossary

- `ECS`: 当前使用的云服务器
- `release`: 一次前端静态资源发布目录
- `current`: 当前线上版本软链
- `bootstrap conf`: 证书签发前的 HTTP Nginx 配置
- `live feed`: 前端直连 devnet RPC 的实时模式
- `mock feed`: 前端本地模拟数据模式
- `keeper`: 持续执行 `set_price + sync_pool` 的常驻进程
- `mock-oracle`: 仅 devnet 使用的测试预言机程序
